function convertPxX(px) {
    return (px / windowWidth) * 100;
}

function convertVwX(vw) {
    return (vw / 100) * windowWidth;
}

function convertPxY(px) {
    return (px / windowHeight) * 100;
}

function convertVwY(vw) {
    return (vw / 100) * windowWidth;
}